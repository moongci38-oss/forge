# Skill Test Report: game-logic-visualize

**Date**: 2026-03-30
**Skill**: game-logic-visualize (v2)
**Model**: sonnet
**Status**: MIXED (PASS+WARN+FAIL)

---

## Executive Summary

| 카테고리 | 결과 | 비율 |
|---------|------|------|
| PASS (기초 구조) | 8/8 | 100% |
| WARN (세부 명확성) | 4/4 | 100% |
| FAIL (구현 불가) | 3/3 | 100% |
| **종합 점수** | **65/100** | - |

---

## 검증 상세

### 1. PASS: 프롬프트 기초 구조 (8/8)

#### ✓ 역할 명확화
- **L9**: "당신은 게임 로직을 Mermaid·Draw.io·HTML 시뮬레이터로 시각화하는 게임 기획 시각화 전문가입니다"
- 역할과 책임이 명확하고 구체적

#### ✓ 컨텍스트 명시
- **L10**: GDD/S4 기획서 작성, Spec 검증, 밸런싱 사전 검증 시 호출
- 사용 시나리오 3가지 명확

#### ✓ 출력 정의
- **L11**: Mermaid·Draw.io·HTML 시뮬레이터를 지정 경로에 저장
- 출력 타입과 위치 정의

#### ✓ 워크플로우 단계화
- **L28-37**: 4단계로 명확히 구분
  - 입력 → Step 1 (로직 유형 판별) → Step 2 (도구 선택) → Step 3 (생성) → Step 4 (출력)
- 각 단계의 책임이 명확

#### ✓ 도구 선택 기준
- **L50-60**: flowchart로 조건부 선택 로직 정의
  - 노드 수에 따른 Mermaid vs Draw.io 구분
  - 시뮬레이션 유형별 Playground 사용

#### ✓ 유형별 생성 상세 (L62-138)
- FSM (stateDiagram-v2 예시)
- 확률 테이블 (Playground 프롬프트 템플릿)
- 전투 공식 (HTML 시뮬레이터)
- 경제 시스템 (flowchart Mermaid)
- 스킬 트리 (트리 구조)
- 인벤토리 ER (classDiagram)
- 각 유형별 구체적 예시 포함

#### ✓ 저장 경로 규칙
- **L143-149**: 날짜+이름+타입 패턴 명시
  - `{YYYY-MM-DD}-{name}-{type}.{ext}`

#### ✓ 마크다운 삽입 형식
- **L151-169**: 상대경로 참조 예시 제공
  - Mermaid: 마크다운에 직접 삽입
  - HTML: 상대경로 링크

---

### 2. WARN: 세부 명확성 부족 (4/4)

#### ⚠️ LOGIC_TYPE 일관성 부족
**위치**: L43 vs L62-138
**문제**:
- Step 1에서 정의된 LOGIC_TYPE: `fsm | probability | combat | economy | skill-tree | inventory` (snake_case, 영문 소문자)
- 실제 유형별 생성: "FSM", "확률 테이블", "전투 공식" (한글/대문자 혼용)
- 입력 처리 시 정규화 필요

**영향**: 중간 (로직 흐름에 영향, 일관성 문제)

#### ⚠️ Playground 스킬 호출 문법 모호
**위치**: L85-95
**문제**:
- "playground:playground 스킬을 호출하여"라고만 명시
- 실제 skill invoke 문법 불명확
- 프롬프트 템플릿만 예시되고, 실제 통합 방식 미정의
- 예시: playground에 전달할 프롬프트를 어떻게 구성할지, 응답 형식은 무엇인지 불명확

**영향**: 높음 (가챠 시뮬레이터, 전투 공식 등 50%의 기능이 이 호출에 의존)

#### ⚠️ Draw.io MCP 호출 방식 불명확
**위치**: L79, L112-129
**문제**:
- "open_drawio_mermaid" 또는 "open_drawio_xml"로 호출한다고만 명시
- 이들이 MCP 도구인지, 함수명인지, skill invoke 형식인지 불명확
- Playground와 달리 MCP 도구 네이밍이 일관성 없음

**영향**: 높음 (FSM 15+ 상태, 경제 시스템 등 기능 구현 불가)

#### ⚠️ Assessment 기준의 적용성 문제
**위치**: assessment.md L22-26
**문제**:
- 5개 기준이 모든 입력에 동일하게 적용됨
- input_1 (FSM): 상태/전이만 있고 **수치 없음** → 기준 #3 ("수치/확률 반영")이 적용 불가
- input_2 (가챠): 수치 있음 → 적용 가능
- input_3 (스킬 트리): 개수 있음 → 적용 가능
- 따라서 기준이 입력마다 달라져야 하는데, 현재는 고정

**영향**: 중간 (평가 기준의 신뢰성 저하)

---

### 3. FAIL: 구현 불가능 영역 (3/3)

#### ✗ FAIL 1: 출력 경로 멀티프로젝트 미지원
**위치**: L143-149
**문제**:
```
docs/assets/game-logic/
├── {YYYY-MM-DD}-{name}-fsm.md
└── ...
```
- 상대경로 사용으로 프로젝트 루트 가정
- Forge 규칙 (forge-core.md): **"forge/ = 시스템 / forge-outputs/ = 결과물"**
- 이 스킬이 GodBlade/Portfolio 등 **다른 프로젝트**에서 호출될 경우 경로 완전히 달라짐

**필요 사항**:
1. 프로젝트 루트 감지 로직
2. forge-workspace.json 참조 (프로젝트별 경로 매핑)
3. forge-outputs/ 경로 동적 계산
4. 프로젝트 컨텍스트 없을 시 fallback 정의

**영향**: CRITICAL (다양한 프로젝트에서 독립 실행 불가능)

#### ✗ FAIL 2: Playground/Draw.io 실제 통합 미정의
**위치**: L85-95, L79, L112-129
**문제**:
- Playground 스킬의 입력/출력 형식 미정의
  - 프롬프트를 어떻게 구성하면 playground가 인터랙티브 HTML을 생성하는가?
  - 응답 형식은 HTML 파일 경로인가, 코드인가?
- Draw.io MCP 호출 방식 불명확
  - MCP 도구 네이밍 확인 필요
  - XML 직접 생성 vs Mermaid 변환 선택 기준 불명확
- 실제 테스트 없음

**필요 사항**:
1. Playground 스킬과의 정확한 인터페이스 정의
2. Draw.io MCP 도구 확인 및 호출 예시
3. 파이프라인 통합 테스트

**영향**: CRITICAL (게임 로직 50% 기능 구현 불가)

#### ✗ FAIL 3: GDD/기획서 데이터 추출 방법 미정의
**위치**: L30-47 (Step 1)
**문제**:
- "GDD/기획서에서 추출" 또는 "직접 입력"이라고만 명시
- 실제 추출 방법 미정의:
  - 마크다운 텍스트 파싱?
  - JSON 구조화된 입력?
  - 이미지 분석 (GDD 스크린샷)?
  - 사용자 인터랙티브 입력 폼?
- Step 1 ("로직 유형 판별")에서 이 데이터를 어떻게 처리할지 불명확

**필요 사항**:
1. 입력 형식 명확화 (구조화된 데이터 스키마)
2. 파싱 로직 정의
3. 예시 입력/출력

**영향**: 높음 (GDD 기반 자동화 불가능)

---

## 모델 설정 검토

**현재 설정**: `model: sonnet`
**권장**: `model: opus`

### 이유
이 스킬이 수행해야 할 작업:
- Playground 스킬 호출 시 프롬프트 구성 (창의적 작성)
- Draw.io XML 직접 생성 (구조적 이해 + 형식 생성)
- 복잡한 FSM/경제 플로우 분석 (비전 + 추론)
- 다중 유형 판별 및 도구 선택 (추론)

Sonnet은 고속 작업에는 우수하지만, XML 생성, 복잡한 구조 설계, 다중 경로 분기 처리는 Opus 4.6의 강점입니다.

---

## 평가 기준 (Assessment) 재검토

현재 assessment.md의 평가 기준 5개가 모든 입력에 동일하게 적용되는데, 이는 문제가 있습니다.

### 개선안
```
input_1 (FSM): 기준 1, 2, 4, 5 적용 (기준 3 제외 - 수치 없음)
input_2 (확률): 기준 1, 2, 3, 4, 5 모두 적용
input_3 (스킬트리): 기준 1, 2, 3, 4, 5 모두 적용
```

---

## 권장사항 (Priority Order)

| Priority | Item | 해결 시간 | 복잡도 |
|----------|------|---------|--------|
| **CRITICAL** | 출력 경로 멀티프로젝트 지원 | 30분 | 중간 |
| **CRITICAL** | Playground/Draw.io 실제 통합 테스트 | 1시간 | 높음 |
| **HIGH** | GDD 데이터 추출 방법 명확화 | 45분 | 중간 |
| **HIGH** | 모델 설정 sonnet → opus 변경 | 5분 | 낮음 |
| **MEDIUM** | LOGIC_TYPE 일관성 (snake_case) | 15분 | 낮음 |
| **MEDIUM** | Assessment 기준 입력별 커스터마이징 | 15분 | 낮음 |

---

## 결론

**프롬프트 구조는 탄탄합니다** - 역할/컨텍스트/워크플로우/도구 선택 기준이 모두 명확하게 정의되어 있습니다.

**그러나 구현 불가능한 영역이 3가지 있습니다**:
1. 멀티프로젝트 경로 지원 부재
2. 외부 스킬(Playground, Draw.io) 통합 미정의
3. GDD 데이터 추출 방법 미정의

**이 3가지를 해결하면** 게임 기획자가 GDD/Spec 작성 시 자동으로 로직을 시각화하는 강력한 도구가 될 것입니다.

---

## 테스트 입력 상태

현재 assessment.md에 정의된 3가지 테스트 케이스는 다양한 유형을 잘 커버합니다:
- ✓ input_1: FSM (상태 머신) - 기본 구조
- ✓ input_2: 확률 테이블 (가챠) - 수치 시뮬레이션
- ✓ input_3: 스킬 트리 - 계층 구조

다만 **실제 실행 없이** 프롬프트 구조만 검증한 상태입니다.
