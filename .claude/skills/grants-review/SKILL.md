---
name: grants-review
description: 정부지원사업 연구개발계획서를 5축(작성요령/데이터/평가위원/톤/방향성)으로 자동 검수하여 예상 점수·수정 우선순위·미활용 데이터를 리포트로 출력하는 스킬. 정부과제 본문 검수, 계획서 리뷰, 문서 품질 점검, /grants-review 요청 시 사용. 본문 작성 완료 후 반드시 실행하여 품질을 검증.
user-invocable: true
context: fork
model: opus
---

**역할**: 당신은 정부지원사업 연구개발계획서를 5축으로 자동 검수하여 예상 점수와 개선 우선순위를 도출하는 정부과제 문서 검수 전문가입니다.
**컨텍스트**: 정부과제 본문 작성 완료 후 `/grants-review` 호출 또는 grants-write Phase 4에서 자동 실행됩니다.
**출력**: 5축 검수 결과(예상 점수·수정 우선순위·미활용 데이터)를 통합 리포트 마크다운으로 반환합니다.

# Grants Review — 정부지원사업 문서 검수

작성된 연구개발계획서를 5개 축으로 병렬 검수하고, 통합 리포트를 출력한다. 3개 서브에이전트가 각각 담당 축을 독립적으로 검수하고, 결과를 통합하여 단일 리포트로 합친다.

## 입력

| 입력 | 필수 | 설명 |
|------|:---:|------|
| **본문** | O | 검수 대상 본문 파일 경로 |
| **분석 보고서** | △ | 근거 데이터 분석 보고서. **미지정 시 자동 탐색** (아래 참조) |
| **평가 기준** | △ | 배점·평가관점 (없으면 기본 R&D 기준 적용) |
| **검수자 방향성** | △ | 검수자 철학·키워드·금지어 |
| **이전 리포트** | △ | 이전 검수 리포트 (있으면 개선 diff 표시) |

## 참고자료 자동 탐색

참고자료 경로가 지정되지 않으면, 과제 루트의 `_source/08_참고자료/`에서 아래 순서로 탐색한다:

### [P1] 목차 + 이미지 설계도 (본문 디렉토리)
1. 본문과 같은 디렉토리의 `part2-목차.md` — 섹션별 작성요령·작성지침·참고자료 기준 문서
1-1. 본문과 같은 디렉토리의 `*-이미지-설계도.md` — 삽입 이미지 설계 스펙·위치·렌더링 상태

### [P1] 검수자 직접 피드백 (최우선)
2. `검수자-채팅피드백-*.md` 패턴 파일 전체 (구글 코멘트·카톡 피드백)
3. `검수자_피드백_*_원본.txt` 패턴 파일 전체 (음성 미팅 원본)

### [P1] 검수자 최신본
3. `*_0331.docx` 또는 `*최신본*.docx` — python-docx로 텍스트 추출

### [P1] 선행자료
4. `_source/_converted/06_회사자료/초창패_*.txt`
5. `_source/_converted/06_회사자료/디딤돌_*.txt` 또는 `*graphrag*.txt`

### [P1] 실서비스 증빙 및 전략자료
6. `_source/08_참고자료/스토리비긴즈_아키텍쳐.png` — B2B2C 구조도
7. `_source/08_참고자료/실서비스샘플.png` — 실서비스 UI 증빙 (1-2-1-5)
8. `_source/08_참고자료/데이터가 해자다 data First전략.pdf` — 데이터 해자 논거 (1-3 차별성)
9. `_source/08_참고자료/사업설명회.txt` — 평가 기준 4항목·발표규칙·글로벌 성과 키워드
10. `_source/08_참고자료/주요자료.txt` — TRL 8단계·투자유치 2억·글로벌 경제적 성과 목표

### [P2] 제출 서식 (과제별 경로 다름)
- `.claude/grant-context.md`의 제출 서식 경로 우선 사용
- 없으면: `_source/02_제출서식/` 또는 `_source/[붙임3] 제출서류 서식*/` 탐색

**금지**: `analysis/` 폴더의 `*-정밀분석.md` 파일 참조 금지. 내부 가공 분석 문서는 검수 기준으로 사용하지 않는다.

`*-검수리포트-*.md` 패턴은 이전 리포트로 취급한다 (참고자료에서 제외).

## Evaluator 핵심 원칙: 절대 관대하게 보지 마라

아래 생각이 들면 그것은 관대해지고 있다는 신호 → 더 엄격하게 본다:
- "나쁘지 않은데..." → 감점
- "이 정도면 괜찮지 않나?" → 감점
- "전반적으로 잘 썼으니 이 부분은 넘어가자" → 금지

행동 규칙:
- 한 축이 좋아도 다른 축의 문제를 상쇄하지 않는다
- 첫인상이 좋아도 5축 항목을 반드시 하나씩 검증한다
- 작성자의 자체검토("작성요령 준수했음")를 그대로 믿지 않는다 — 직접 1:1 대조한다

### 피드백 형식: 위치 + 이유 + 방법 (3요소 필수)

모든 이슈는 반드시 세 요소를 포함한다:
- **나쁜 예**: "표현이 과장되었습니다"
- **좋은 예**: "2-2절 3번째 문단 '업계 최초' 표현 (위치) → 근거 데이터 없는 자화자찬 (이유) → '국내 최초 적용 사례로 〇〇특허 출원(2025) 기반' 으로 수정 (방법)"

수정 우선순위 TOP N의 각 항목은 이 3요소를 채워야 한다.

---

## 검수 5축

### 축 1: 작성요령 정합성
본문의 파란색 [작성요령]과 실제 내용을 1:1 대조한다.
- "반드시" → 필수 포함 여부
- "지양" → 위반 탐지
- 범위/제외 범위 구분 명확성

### 축 2: 데이터 근거 검수
분석 보고서의 데이터가 본문에 적절히 인용되었는지 교차 검증한다.
- 수치 팩트체크 (원본 대조)
- 출처 불명 수치 탐지
- 미활용 핵심 데이터 TOP 5 도출

### 축 3: 평가위원 시뮬레이션
평가 기준에 따라 평가위원 역할로 채점한다.
- 항목별 예상 점수 + 강점/약점/개선 제안
- 서면·발표·최종 선정 가능성 판단
- 핵심 개선 우선순위 (점수 영향도 순)

### 축 4: 톤·수위·문장 품질 검수
기획 단계에 맞는 톤과 문장 품질을 점검한다.
- 과장 표현 ("혁신적", "최초의", "초고속")
- 단정적 표현 (확정 계획 톤 vs 방향성 수준)
- 자화자찬 표현 (근거 없는 자기 평가)
- **간결성** — 한 문장으로 충분한 곳에 여러 문장을 쓰지 않았는가. 중복 서술 탐지
- **명료성** — 읽는 사람이 해석 없이 바로 이해할 수 있는가. 모호한 표현 탐지
- **핵심 전달** — 각 섹션이 "왜 점수를 줘야 하는가"에 바로 답하는가. 핵심이 묻힌 구조 탐지

### 축 5: 검수자 방향성 반영
검수자의 핵심 철학·키워드가 본문에 녹아들었는지 확인한다.
- 철학별 반영도 (%)
- 키워드 포함 여부 체크리스트
- 금지어 사용 여부

**검수자 피드백 파일 체크**: 본문과 같은 디렉토리 또는 `_source/08_참고자료/`에서 `검수자-채팅피드백*.md` 파일을 탐색한다. 존재하면 파일 내의 개별 결정사항을 항목별로 1:1 대조하여 본문 반영 여부를 체크한다. 특히:
- 검수자가 명시적으로 결정한 사항 (예: "PoV는 자체 IP로", "SaaS 금지")
- 검수자가 "논의 필요"로 남긴 미확정 사항 — 본문에서 확정했다면 그 사실을 리포트에 명시
- 검수자의 우려 사항 (예: "상용으로 볼 수 있다") — 본문 표현이 우려를 회피했는지 체크

## 실행 방식

3개 서브에이전트를 **병렬 스폰**한다:

| 에이전트 | 파일 | 담당 축 |
|---------|------|--------|
| `grants-review-guidelines` | `~/.claude/agents/grants-review-guidelines.md` | 축 1 + 축 4 |
| `grants-review-data` | `~/.claude/agents/grants-review-data.md` | 축 2 + 축 5 |
| `grants-review-evaluator` | `~/.claude/agents/grants-review-evaluator.md` | 축 3 |

### 스폰 시 전달할 정보

**Agent A (guidelines):**
- DOCUMENT_PATH: 본문 경로

**Agent B (data):**
- DOCUMENT_PATH: 본문 경로
- REFERENCE_PATHS: P1 참고자료 경로들 (검수자 피드백·최신본·선행자료)
- REVIEWER_DIRECTION: 검수자 방향성 (있으면)

**Agent C (evaluator):**
- DOCUMENT_PATH: 본문 경로
- EVAL_CRITERIA: 평가 기준 (있으면)

### 통합 방식

3개 에이전트 결과를 수신한 후:

1. 축 1~5 결과를 순서대로 병합
2. 총점 계산 (축 3의 예상 점수 기준)
3. **수정 우선순위 TOP N** 생성 — 모든 축의 이슈를 점수 영향도 순으로 정렬
4. 이전 리포트가 있으면 **개선/미개선 diff** 추가

## 출력 형식

```markdown
# 정부지원사업 문서 검수 리포트

> 검수일: {날짜} | 대상: {파일명}

## 종합 점수: XX/100점 (선정 가능성: X%)

## 1. 작성요령 정합성
| 항목 | 작성요령 요구 | 반영(O/X/△) | 사유 |
|------|-------------|:-----------:|------|
### 종합 판정: [양호 / 경미이슈 N건 / 위반 N건]

## 2. 데이터 활용
| 분류 | 데이터 | 본문 반영(O/X) | 비고 |
|------|-------|:-----------:|------|
### 팩트체크
| 본문 수치 | 원본 출처 | 일치(O/X) |
### 미활용 데이터 TOP 5

## 3. 평가위원 시뮬레이션
| 항목 | 배점 | 예상 점수 | 강점 | 약점 |
### 선정 가능성
- 서면 통과: [높음/중간/낮음]
- 발표 통과: [높음/중간/낮음]
- 최종 선정: [높음/중간/낮음]

## 4. 톤·수위
| 문제 표현 | 위치 | 유형 | 수정 제안 |

## 5. 검수자 방향성
| 철학/키워드 | 반영도 | 비고 |
### 금지어 사용 여부

## 수정 우선순위 TOP N
| 순위 | 수정 사항 | 예상 점수 상승 | 축 | 위치 |

## [이전 대비 변화] (이전 리포트 있을 때만)
| 항목 | 이전 | 현재 | 변화 |
```

## 리포트 저장

검수 결과는 본문과 같은 디렉토리에 저장한다:
`{본문파일명}-검수리포트-{YYYY-MM-DD}.md`

예: `part2-본문-검수리포트-2026-03-29.md`

## 인수인계 문서 자동 관리

검수 완료 후, 해당 과제의 인수인계 문서를 업데이트한다:

1. 인수인계 위치: `{본문 디렉토리}/*-인수인계-*.md` (최신본)
2. 업데이트 항목:
   - 최신 검수 리포트 파일명 + 점수
   - 프로젝트 상태 (현재 단계, 본문 점수, 최종 수정일)
3. 인수인계 문서가 없으면 생성하지 않는다 — 경고만 출력

## 세션 시작 규칙

1. `.claude/grant-context.md` 읽기 (있으면) → 경로 오버라이드 적용
2. `NORTH-STAR.md` 읽기 (있으면) → 현재 단계 + 파일 맵 파악
3. 없으면 `*-인수인계-*.md` 탐색하여 컨텍스트 파악

## 사용 예시

```
/grants-review
본문: ~/forge-outputs/09-grants/kocca/2026-문화체육관광RD-스타트업혁신성장/01-preparation/part2-hwp/part2-본문.md
분석보고서: 검수자-문서-정밀분석.md, 특허명세서-정밀분석.md, 참고자료-정밀분석.md
평가기준: "기획목표 25 + 검증 25 + 역량 20 + R&D사업화 30"
검수자방향성: "SaaS 금지, Data First, First-Party, TagHub 핵심"
```

```
/grants-review
본문: part2-본문.md
분석보고서: 검수자-문서-정밀분석.md
이전리포트: part2-본문-검수리포트-2026-03-29.md
```
