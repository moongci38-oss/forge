---
title: "컨텍스트 윈도우 관리 규칙"
id: forge-context-management
impact: MEDIUM
scope: [forge]
tags: [context, compact, token, session, mcp]
requires: [forge-context-engineering]
section: forge-context
audience: dev
impactDescription: "미준수 시 컨텍스트 70% 초과로 응답 품질 비선형 저하, 자동 수정 루프에서 토큰 고갈, MCP 도구 정의로 가용 토큰 잠식"
enforcement: flexible
---

# Context Window Management Rules

## 핵심 원칙

200K 토큰 컨텍스트 중 **70% (약 140K)**를 실질적 상한선으로 관리한다.
성능 저하는 마지막 20%에서 비선형 가속되므로, 70% 초과 전에 대응한다.

## /compact 트리거 시점

| 시점 | 조건 | 행동 |
|------|------|------|
| Phase 2 → 3 전환 | Spec/Plan 작성 완료 후 구현 시작 전 | `/compact` 실행 권장 |
| AI Check 3.5 반복 | 자동 수정 루프 **3회 초과** | `/compact` 후 재시작 |
| 대량 파일 탐색 후 | Grep/Read 결과가 누적되었을 때 | `/compact` 실행 권장 |
| 체감 성능 저하 | 응답 품질 하락 감지 시 | `/compact` 즉시 실행 |

## 컨텍스트 절약 전략

### 1. 서브에이전트 위임 우선

대용량 코드베이스 탐색, 멀티파일 검색은 **Explore 서브에이전트에 위임**한다.
서브에이전트는 독립 컨텍스트를 사용하므로 메인 세션 컨텍스트를 소비하지 않는다.

### 2. 파일 읽기 최소화

- 전체 파일 대신 `offset`/`limit`으로 필요 부분만 읽기
- 이미 읽은 파일 내용을 반복 요청하지 않기
- 대형 로그/빌드 출력은 `head_limit`으로 제한

### 3. MCP 도구 토큰 인지

MCP 서버 상시 로드 시 도구 정의만으로 상당한 토큰 소비.
실제 작업 가용 컨텍스트는 **200K - MCP(도구 정의) - Rules** 기준으로 계산.

### 4. MCP Tool Search 활용

- `/mcp` 명령으로 미사용 MCP 서버 비활성화 → 도구 정의 토큰 절약
- MCP Tool Search (레이지 로딩)가 활성화되면 필요 시점까지 도구 로딩 지연
- `/context` 명령으로 토큰 사용량 상세 브레이크다운 확인

## /clear 사용 기준 (Pre-Clear Sanity Check)

| 판정 | 조건 | 이유 |
|:----:|------|------|
| **금지** | 관련 작업 중간에 `/clear` 사용 | 캐시 무효화로 비용 4배 증가 |
| **허용** | 완전히 다른 주제로 전환할 때만 | 이전 컨텍스트가 새 작업에 무관 |
| **권장** | 같은 프로젝트 내 작업은 `/compact`로 압축 후 이어서 진행 | 캐시 유지 + 컨텍스트 보존 |

### /clear 남용 방지 (금지 패턴)

`/clear`를 사용하기 전에 아래 체크리스트를 통과해야 한다. **하나라도 해당하면 `/compact` 또는 세션 유지**를 선택한다.

| # | 질문 | Yes | No |
|:-:|------|:--:|:--:|
| 1 | 현재 진행 중인 작업이 있는가? | 금지 | 진행 |
| 2 | 읽은 파일이나 생성한 코드가 있는가? | 금지 | 진행 |
| 3 | 같은 프로젝트 내에서 다음 페이즈로 진행할 예정인가? | 금지 | 진행 |
| 4 | 이전 컨텍스트가 새 작업에 도움이 될 가능성이 0.1%라도 있는가? | 금지 | 진행 |
| 5 | `/compact` 후 이어서 진행할 수 있는가? | **`/compact` 사용** | 진행 |

**진행 (Proceed with /clear)**: 모든 질문이 No인 경우만. 그 외는 금지.

### /clear 누적 비용

```
/clear 1회당 비용:
- 캐시 무효화: 이전 토큰 비용이 완전히 낭비됨 (4배 추가 비용)
- 컨텍스트 손실: MEMORY.md 수동 갱신 필요
- 세션 쇠퇴: 이전 결정사항, 계획, 코드 레퍼런스 전부 손실

예시:
- Phase 2 완료 후 /clear → Phase 3 재진입: Phase 2 생성물(Spec, Plan) 컨텍스트 완전 손실
  → Phase 3 구현 시 Spec 재읽기 필요 → 추가 토큰 낭비
  → 대신: /compact 후 계속 진행하면 모두 해결
```

### /clear 안전 대체재

| 상황 | /clear 사용 | 권장 대체 | 이유 |
|------|:---------:|---------|------|
| 컨텍스트 >70% | ❌ | `/compact` | 핵심 정보 보존 + 토큰 절약 |
| Phase 전환 | ❌ | `/compact` | Spec/Plan 캐시 유지 |
| 프로젝트 변경 (같은 repo) | ❌ | `--rename` + `/compact` | 세션명 갱신 후 진행 |
| 완전 다른 프로젝트 | ⚠️ | `--continue` with new project | 이전 세션은 `/resume`로 되돌리기 가능 |

## 세션 관리 규칙

| 명령 | 용도 | 사용 시점 |
|------|------|----------|
| `/rename {feature-name}` | 세션에 설명적 이름 부여 | 작업 시작 시 (필수 습관) |
| `--continue` | 가장 최근 대화 이어하기 | 세션 재개 시 |
| `--resume` | 대화형 세션 선택기 | 특정 과거 세션 재개 시 |

### 세션 간 영속화

- 세션 종료 전 **git commit으로 작업 영속화** (세션 간 상태 보존)
- `/compact` 후에도 핵심 결정사항은 MEMORY.md에 기록
- 장기 분석은 서브에이전트 `resume` (agentId)으로 이어서 실행 가능

## AI 에이전트 행동 규칙

1. Phase 전환 시 `/compact` 실행 여부를 판단한다
2. AI Check 3.5 자동 수정 3회 초과 시 반드시 `/compact` 후 재시작
3. 3개 이상 파일 탐색 시 Explore 서브에이전트 위임을 우선 고려한다
4. 컨텍스트 부족 징후 (응답 누락, 반복 질문) 감지 시 즉시 `/compact`
5. 작업 시작 시 `/rename`으로 세션 이름 부여를 고려한다
6. `/context`로 토큰 사용 현황을 주기적으로 확인한다
